# Telegram

Telegram is an optional Owner communication channel.

When configured, the system can:

- send a message to the Owner
- notify the Owner when a task completes
- ask the Owner for a decision
- notify the Owner when the cycle budget is exhausted

Credentials are supplied through environment variables.

Never write the Telegram bot token into memory.

Never expose the Telegram bot token to the model.